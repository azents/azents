Azents chart rendered successfully.

This phase renders the default server, web, adminWeb, Agent Runtime resources, and default-off optional components.

Default-on components:
- server.apiserver
- server.adminserver
- server.worker
- web
- adminWeb

Default-off components:
- server.mcpEgressProxy
- snapshotter

Prepare existing Secrets with these keys before installing:
- {{ .Values.secrets.existingSecrets.auth }}: jwt-secret-key, credential-encryption-key, sentry-dsn, oauth-secret-key
{{ if .Values.server.platformGitHubApp.existingSecret }}
- {{ .Values.server.platformGitHubApp.existingSecret }}: {{ .Values.server.platformGitHubApp.appIdKey }}, {{ .Values.server.platformGitHubApp.privateKeyKey }}, {{ .Values.server.platformGitHubApp.clientIdKey }}, {{ .Values.server.platformGitHubApp.clientSecretKey }} for Platform GitHub App field overlays
{{ end }}
{{ if .Values.server.systemBootstrap.existingSecret }}
- {{ .Values.server.systemBootstrap.existingSecret }}: {{ .Values.server.systemBootstrap.tokenKey }} for the optional zero-user setup token
{{ end }}

Configure adminWeb.publicUrl for the externally visible Admin Web base and web.adminWebUrl to expose the role-gated Main Web link.

Optional components remain default-off. Enabling snapshotter requires privileged workload support, hostPath access to containerd.sock, node scheduling constraints, and registry push credentials. Enabling mcpEgressProxy injects AZ_MCP_PROXY_URL into server workloads.
