# Temporary Requirements: Runtime Control Web endpoint minimal fix

- Scope: fix the deployed Runtime Web Runner Control connection failure with the smallest safe code change.
- Root cause evidence: Helm advertises `$(AZ_RUNTIME_CONTROL_INSTANCE_ID).runtime-control-headless.$(AZ_RUNTIME_CONTROL_POD_NAMESPACE).svc:8030`; live Deployment Pods have no `hostname/subdomain`, the headless Service exposes `trusted-web` on 8032, and Runner logs show DNS failure for the advertised per-Pod `:8030` address.
- Required change: advertise the existing stable `runtime-control.<namespace>.svc.cluster.local:8030` Service for Runner Web Control connections.
- Preserve: current Runtime Web protocol behavior, trusted owner address, Service ports, and all other deployment values.
- Verification: add/update Helm render assertion for the stable endpoint, run focused chart/render tests and relevant CI checks.
- Delivery: create a separate branch and PR; do not apply/sync/restart/delete/scale/rollback live resources.
- Cleanup: delete this temporary requirements file after the PR change is recorded.
