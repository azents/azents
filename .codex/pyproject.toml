[project]
name = "azents-codex-hooks"
version = "0.1.0"
description = "Repo-local Codex hooks for Azents"
requires-python = ">=3.14"
dependencies = [
    "python-frontmatter==1.1.0",
]

[tool.uv]
package = false
resolution = "lowest-direct"

[dependency-groups]
dev = []
